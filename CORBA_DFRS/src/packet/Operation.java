package packet;

public enum Operation {
	BOOK_FLIGHT,
	BOOKED_FLIGHTCOUNT,
	EDIT_FLIGHT,
	TRANSFER_RESERVATION,
	
	REPLICA_ALIVE,
	REPLICA_REBOOT,
	REPLICA_CRASH,
	REPLICA_KILL,
	
	OPERATION_LOG,
	EXECUTE_OPERATION_LOG
}
